const {currentPublicOffer}=require('../server/public-offer');
module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const offer=await currentPublicOffer();res.setHeader('Cache-Control','public, max-age=60, s-maxage=120');return res.status(200).json({offer:offer?{id:offer.id,eyebrow:offer.eyebrow,title:offer.title,description:offer.description,cta_label:offer.cta_label,trial_days:offer.trial_days,discount_percent:offer.discount_percent,ends_at:offer.ends_at,source:offer.source||'admin'}:null})}catch{return res.status(200).json({offer:null})}};
