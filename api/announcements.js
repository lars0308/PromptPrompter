const {serviceFetch}=require('../server/admin');
module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const now=encodeURIComponent(new Date().toISOString()),result=await serviceFetch(`/rest/v1/sitebrief_announcements?select=id,title,body,level,starts_at,ends_at&active=eq.true&starts_at=lte.${now}&or=(ends_at.is.null,ends_at.gt.${now})&order=created_at.desc&limit=5`);res.setHeader('Cache-Control','public, max-age=30, s-maxage=60');return res.status(200).json({announcements:result.data||[]})}catch{return res.status(200).json({announcements:[]})}};
