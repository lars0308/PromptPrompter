const crypto=require('crypto');
const {SUPABASE_URL}=require('../server/supabase-user');
function rawBody(req){return new Promise((resolve,reject)=>{const chunks=[];req.on('data',chunk=>chunks.push(Buffer.from(chunk)));req.on('end',()=>resolve(Buffer.concat(chunks)));req.on('error',reject)})}
function verify(body,header,secret){const parts=Object.fromEntries(String(header||'').split(',').map(x=>x.split('='))),timestamp=parts.t,signature=parts.v1;if(!timestamp||!signature)return false;const expected=crypto.createHmac('sha256',secret).update(`${timestamp}.${body.toString('utf8')}`).digest('hex');if(expected.length!==signature.length)return false;return crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(signature))&&Math.abs(Date.now()/1000-Number(timestamp))<300}
async function updateSubscription(filter,payload){const key=process.env.SUPABASE_SERVICE_ROLE_KEY||'';if(!key)throw new Error('SUPABASE_SERVICE_ROLE_KEY fehlt');const response=await fetch(`${SUPABASE_URL}/rest/v1/sitebrief_subscriptions?${filter}`,{method:'PATCH',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify({...payload,updated_at:new Date().toISOString()})});if(!response.ok)throw new Error('Abo konnte nicht gespeichert werden')}
async function handler(req,res){if(req.method!=='POST')return res.status(405).end();try{const secret=process.env.STRIPE_WEBHOOK_SECRET||'';if(!secret)throw new Error('Webhook ist nicht eingerichtet');const raw=await rawBody(req);if(!verify(raw,req.headers['stripe-signature'],secret))return res.status(400).json({error:'Invalid signature'});const event=JSON.parse(raw.toString('utf8')),object=event.data?.object||{};if(event.type==='checkout.session.completed'){const userId=object.client_reference_id||object.metadata?.user_id,plan=object.metadata?.plan;if(userId&&['pro','ultimate'].includes(plan))await updateSubscription(`user_id=eq.${encodeURIComponent(userId)}`,{plan,status:'active',provider:'stripe',provider_customer_id:object.customer,provider_subscription_id:object.subscription})}else if(event.type==='customer.subscription.updated'||event.type==='customer.subscription.deleted'){const plan=object.metadata?.plan,status=event.type.endsWith('.deleted')?'canceled':(['active','trialing'].includes(object.status)?object.status:object.status==='past_due'?'past_due':'canceled');await updateSubscription(`provider_subscription_id=eq.${encodeURIComponent(object.id)}`,{...(plan&&['pro','ultimate'].includes(plan)?{plan}:{}),status,current_period_end:object.current_period_end?new Date(object.current_period_end*1000).toISOString():null})}return res.status(200).json({received:true})}catch(error){return res.status(500).json({error:error.message||'Webhook failed'})}}
module.exports=handler;
module.exports.config={api:{bodyParser:false}};
